name: intro pipeline

on: 
  workflow_dispatch:

jobs:
  basic-workflow: 
    runs-on: ubuntu-latest
    steps:
      - name: first step 
        run: |
          echo "hello world" > file.txt
          cat file.txt
          mv file.txt file2.txt
          ls

          

  